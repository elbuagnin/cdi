"use strict";
const nlp = require('compromise');
nlp.extend(require('compromise-paragraphs'));

module.exports = function cdi(name, description) {



   const preppedDoc = pos + m;
   console.log(preppedDoc);
   return (preppedDoc);

}
